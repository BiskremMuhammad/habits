/**
 * @author Muhammad Omran
 * @date 08-05-2021
 * @description implement a utility function that will do blah blah blah..
 */

/**
 * this is an example of a utilit function, and should has it's documentation like this one
 *
 * @param {number} a the first number
 * @param {number} b the second number
 * @returns {boolean} wheather a is larger than b or not
 */
export const utilExample = (a: number, b: number): boolean => {
  return a > b;
};
